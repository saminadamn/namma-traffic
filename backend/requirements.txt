fastapi==0.111.0
uvicorn[standard]==0.30.1
pydantic==2.7.1
pydantic-settings==2.3.0
catboost>=1.2.0
xgboost==2.0.3
scikit-learn==1.5.0
pandas==2.2.2
numpy==1.26.4
shap==0.45.1
joblib==1.4.2
python-multipart==0.0.9
httpx==0.27.0

# ── Added for Priority 1 (RBAC / auth / PostgreSQL) ──
sqlalchemy==2.0.30
alembic==1.13.1
psycopg2-binary==2.9.9
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
# bcrypt is pinned below 4.1 deliberately: passlib 1.7.4 reads a
# bcrypt.__about__ attribute that was removed in bcrypt>=4.1, causing a
# loud AttributeError at hash time. Pinning avoids that known break.
pytest==8.2.2

# ── Added for Priority 2 (PostGIS-backed incidents/reports, WebSockets) ──
GeoAlchemy2==0.15.2

# ── Added for multilingual advisory (Gemini Flash generation + Sarvam AI translation) ──
google-generativeai>=0.7.2
sarvamai>=0.1.28

# ── Added for photo upload (Cloudinary free tier — 25 GB storage) ──
cloudinary>=1.36.0

# ── Rate limiting (no Redis needed) ──
slowapi==0.1.9

# ── Observability ──
# Pin to 5.11.2 — versions >=7 drag in starlette>=1.x which breaks fastapi 0.111.0
prometheus-fastapi-instrumentator==5.11.2

# ── Redis (optional — Upstash free tier for token blacklist + perm cache) ──
# redis>=5 ships async support natively; the [asyncio] extra no longer exists in >=5
redis>=5.0.0

# ── ARQ background jobs (reverse geocoding, scheduled hotspot recalculation) ──
arq>=0.25.0
